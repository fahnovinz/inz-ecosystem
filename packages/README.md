# INZ Ecosystem Packages

Standalone tools live here as the ecosystem grows. Each package is self-contained with its own README.

## Planned

- `github-stats` — profile & repo analytics (currently in root CLI)
- `repo-health` — CI, license, and stale issue checker
- `inz-dotfiles` — dev environment configs